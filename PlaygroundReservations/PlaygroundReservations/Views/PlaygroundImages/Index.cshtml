@model IEnumerable<PlaygroundReservations.Models.PlaygroundImage>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Ново", "Create")
</p>
<table>
    <tr>
        <th></th>
        <th>
            Path
        </th>
        <th>
            Playground
        </th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink("редактирай", "Edit", new { id=item.PlaygroundImageId }) |
            @Html.ActionLink("детайли", "Details", new { id=item.PlaygroundImageId }) |
            @Html.ActionLink("изтрий", "Delete", new { id=item.PlaygroundImageId })
        </td>
        <td>
			@item.Path
        </td>
        <td>
			@(item.Playground == null ? "None" : item.Playground.Name)
        </td>
    </tr>
}

</table>


