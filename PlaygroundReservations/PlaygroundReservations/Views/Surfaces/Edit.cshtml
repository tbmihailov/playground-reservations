@model PlaygroundReservations.Models.Surface

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)
    <fieldset>
        <legend>Surface</legend>

        @Html.HiddenFor(model => model.SurfaceId)
		@Html.Partial("_CreateOrEdit", Model)

        <p>
            <input type="submit" value="Запиши" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("към списък", "Index", null, new { @class = "back-button" })
</div>
